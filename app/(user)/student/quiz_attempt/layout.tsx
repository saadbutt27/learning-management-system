export const metadata = {
  title: "Quiz - LMS",
  description: "Quiz Attempt for Student in Learning Management System",
};

export default function MyLayout({ children }: { children: React.ReactNode }) {
  return <div>{children}</div>;
}
