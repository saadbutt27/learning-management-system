export const metadata = {
    title: "Create Accounts Admin - LMS",
    description: "Create courses, student and teacher accounts page of Admin for Learning Management System",
  };
  
  export default function MyLayout({ children }: { children: React.ReactNode }) {
    return <section>{children}</section>;
  }
  